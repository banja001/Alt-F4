<Window x:Class="booking.View.OwnerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:booking.View" xmlns:booking="clr-namespace:booking"
        mc:Ignorable="d"
        Title="OwnerWindow" Height="749" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="0,97,0,0">


        <Button Content="Add" Command="{Binding AddAccommodationCommand}" HorizontalAlignment="Left" Margin="23,124,0,0" VerticalAlignment="Top" Height="32" Width="136"/>
        <Button Name="RateGuests" Command="{Binding RateGuestsCommand}" Content="Rate guests" HorizontalAlignment="Left" Margin="189,174,0,0" VerticalAlignment="Top" RenderTransformOrigin="-0.842,-1.682" Height="24" Width="78">
            <Button.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="0.362"/>
                    <TranslateTransform X="0.334" Y="-0.659"/>
                </TransformGroup>
            </Button.RenderTransform>
        </Button>

        <DataGrid x:Name="DataGridRaatings" ItemsSource="{Binding ListToRate, UpdateSourceTrigger=PropertyChanged}"
                  SelectedItem="{Binding SelectedItem, UpdateSourceTrigger=PropertyChanged}" SelectionMode="Single"
                  IsReadOnly="True" AutoGenerateColumns="False" Margin="10,232,81,51" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Guest" Width="90" Binding="{Binding GuestName}"/>
                <DataGridTextColumn Header="Accommodation" Width="120" Binding="{Binding AccommodationName}"/>
                <DataGridTextColumn Header="Start date" Width="90" Binding="{Binding StartDate}"/>
                <DataGridTextColumn Header="End date" Width="90" Binding="{Binding EndDate}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Command="{Binding ViewRatingsCommand}" Content="View Rating" HorizontalAlignment="Left" Margin="182,124,0,0" VerticalAlignment="Top" Height="44" Width="99" />
        <Label x:Name="AverageLabel" Content="" HorizontalAlignment="Left" Margin="23,31,0,0" VerticalAlignment="Top" Width="188" Background="Gray"/>
        <Button Command="{Binding ReservationChangeCommand}" Content="ReservationChange" HorizontalAlignment="Left" Margin="23,172,0,0" VerticalAlignment="Top" />
        <Label x:Name="SuperOwnerLabel" Content="" HorizontalAlignment="Left" Margin="23,75,0,0" VerticalAlignment="Top" Width="188" Background="Gray"/>


    </Grid>
</Window>
